import { generateMetadata } from '../../lib/metadata';

export const metadata = generateMetadata({
  title: 'Tours em Londres',
  description: 'Conheça nossos tours personalizados em Londres e região com motorista brasileiro',
  path: '/tours',
});