import { generateMetadata } from '../../lib/metadata';

export const metadata = generateMetadata({
  title: 'Contato',
  description: 'Entre em contato conosco para agendar seu tour personalizado em Londres',
  path: '/contato',
});