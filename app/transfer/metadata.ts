import { generateMetadata } from '../../lib/metadata';

export const metadata = generateMetadata({
  title: 'Transfer',
  description: 'Serviço de transfer de luxo em Londres com motorista brasileiro',
  path: '/transfer',
});